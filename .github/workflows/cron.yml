name: Cron Every 2 Minutes

on:
  schedule:
    - cron: "*/2 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel API Route
        run: curl -X GET "https://pitchtiktok.vercel.app/api/cron"
